import { LoginResponse, RegisterResponse } from "@/types";
import {
    LoginDTO,
  RegisterDTO,
  RefreshTokenDTO,
  AuthTokensDTO,
  VerifyEmailDTO,
  ForgotPasswordDTO,
  ResetPasswordDTO,
  ChangeEmailDTO,
  BeginTwoFactorDTO,
  VerifyTwoFactorDTO,
  UpdateProfileDTO,
  ChangePasswordDTO,
} from '@/types/dtos/auth.dto';
